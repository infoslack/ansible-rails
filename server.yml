---
- name: App Server
  hosts: web
  user: root

  roles:
    - common
    - firewall
    - ruby
    - nginx
    - unicorn
    - upstart
    - postgresql

# Use this block to a different node
#- name: Database Server
#  hosts: db
#  sudo: yes
#  user: root
#
#  roles:
#    - postgresql
