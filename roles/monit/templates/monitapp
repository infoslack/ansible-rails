check file restart.txt with path /var/www/{{app_name}}/current/tmp/restart.txt
  if changed timestamp then restart
  start program = "/sbin/start {{app_name}}"
  stop program = "/sbin/stop {{app_name}}"
